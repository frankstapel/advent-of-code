[project]
name = "advent-of-code"
version = "0.1.0"
description = "Advent of code solutions in Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.0",
    "ruff>=0.8.4",
    "tqdm>=4.67.1",
]
